# Cognitive Complexity Refactor

| Archivo                                          | Funcion           | Complejidad reportada | Objetivo | Helpers extraidos                                                                                                                                                                                          | Comportamiento preservado                                                                                                                         | Pruebas         |
| ------------------------------------------------ | ----------------- | --------------------: | -------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `backend/src/ai/ai.service.ts`                   | `getBuildProfile` |                    22 |    <= 15 | `getBuildTowerBudget`, `getMonitorReserve`, `shouldUseDedicatedGpu`, `getBuildWeights`                                                                                                                     | Perfil de uso, presupuesto de torre, perifericos, preferencia de plataforma y pesos por categoria                                                 | `npm run build` |
| `backend/src/idempotency/idempotency.service.ts` | `run`             |                    20 |    <= 15 | `normalizeKeyOrThrow`, `findOrCreateProcessingRecord`, `resolveExistingRecord`, `validatePayloadHashOrThrow`, `buildCachedResponse`, `executeAndPersist`, `persistSuccessfulResult`, `persistFailedResult` | Rechazo por key ausente, replay con payload igual, conflicto con payload distinto, bloqueo de operaciones en proceso y persistencia de resultados | `npm run build` |
| `backend/src/users/users.service.ts`             | `updateProfile`   |                    19 |    <= 15 | `findProfileUserOrThrow`, `buildProfileUpdateData`, `applyBasicProfileFields`, `getAvailableProfileEmail`, `applyDocumentProfileFields`, `assertDocumentCanBeChanged`, `assertProfileUpdateIsNotEmpty`     | Usuario inexistente, email duplicado, documento inmutable, campos opcionales y respuesta sanitizada                                               | `npm run build` |

## Riesgos pendientes

- No se cambiaron contratos publicos ni DTOs.
- Las pruebas funcionales de chatbot, idempotencia y perfil siguen recomendadas antes de despliegue porque dependen de datos reales y flujos integrados.
